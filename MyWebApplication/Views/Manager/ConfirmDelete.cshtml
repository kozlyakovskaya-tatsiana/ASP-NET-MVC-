@model DataAccessLayer.EntityModels.Manager

@Html.LabelFor(manager => manager.Name, "Manager's name: ") @Model.Name

<br />

@using (Html.BeginForm("Delete", "Manager", FormMethod.Post))
{
    <input type="hidden" name="id" value="@Model.Id" />
    <button type="submit" class="btn">Delete manager</button>
}
