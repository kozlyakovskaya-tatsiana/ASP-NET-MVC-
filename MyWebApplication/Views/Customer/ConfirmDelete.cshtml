@model DataAccessLayer.EntityModels.Customer

@Html.LabelFor(cust => cust.FullName, "Customer's name: ") @Model.FullName

<br />

@using (Html.BeginForm("Delete", "Customer", FormMethod.Post))
{
    <input type="hidden" name="id" value="@Model.Id" />

    <button type="submit" class="btn">Delete customer</button>
}
